/* Windows resources for the OpenChime GUI.
 *
 * The icon is generated by scripts/gen_appicon.py from the app's own mark (the
 * accent rounded square) and the Lucide bell already vendored under ISC — not
 * taken from a stock-icon site, so there is nothing to attribute beyond
 * third_party/lucide/LICENSE and nothing to re-check later.
 */
#include "openchime_res.h"

IDI_APPICON ICON "openchime.ico"

#define OC_RES_DESC "Chat client for an OpenChime server."
#define OC_RES_ORIG "openchime.exe"
#include "version_info.rc"
